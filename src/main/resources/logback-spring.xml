<?xml version="1.0" encoding="UTF-8"?>
<!--
  Profile-aware logging.
  - dev:            pretty console output, DEBUG for our own package.
  - staging/prod:   structured (ECS JSON) console output at INFO (container-friendly; ship stdout to your log stack).
-->
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Local development: chatty, colored, DEBUG on our code. -->
    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="dk.dataleads" level="DEBUG"/>
    </springProfile>

    <!-- Staging & production: INFO, structured (ECS JSON) output to stdout.
         Format comes from 'logging.structured.format.console' (set in
         application-staging/prod.properties), defaulting to ecs. -->
    <springProfile name="staging,prod">
        <springProperty name="STRUCTURED_FORMAT" source="logging.structured.format.console" defaultValue="ecs"/>
        <appender name="STRUCTURED_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="org.springframework.boot.logging.logback.StructuredLogEncoder">
                <format>${STRUCTURED_FORMAT}</format>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="STRUCTURED_CONSOLE"/>
        </root>
        <logger name="dk.dataleads" level="INFO"/>
    </springProfile>

    <!-- Fallback when no profile matches. -->
    <springProfile name="!dev &amp; !staging &amp; !prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
