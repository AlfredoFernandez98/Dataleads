<?xml version="1.0" encoding="UTF-8"?>
<!--
  Profile-aware logging.
  - dev:            pretty console output, DEBUG for our own package.
  - staging/prod:   compact console output at INFO (container-friendly; ship stdout to your log stack).
-->
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Local development: chatty, colored, DEBUG on our code. -->
    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="dk.dataleads" level="DEBUG"/>
    </springProfile>

    <!-- Staging & production: INFO, plain output to stdout. -->
    <springProfile name="staging,prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="dk.dataleads" level="INFO"/>
    </springProfile>

    <!-- Fallback when no profile matches. -->
    <springProfile name="!dev &amp; !staging &amp; !prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
